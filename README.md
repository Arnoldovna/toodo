# make sure you have the full Ruby development environment installed

# install jekyll and bundler gems

gem install jekyll bundler

# install bundle

bundle install

# run locally

bundle exec jekyll serve
